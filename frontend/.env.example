# ============================================================
#  Variables del FRONTEND (Vite)
# ============================================================
#  IMPORTANTE: solo las que empiezan con VITE_ llegan al navegador.
#  NUNCA pongas secretos acá: todo lo del frontend es público.
# ============================================================

# URL base del backend (la API REST).
VITE_API_URL=http://localhost:4000
